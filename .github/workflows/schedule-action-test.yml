name: schedule222
run-name: Execution a workflow by schedule

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  hello-spring-boot:
    runs-on: ubuntu-latest
    steps:
      - name: Every 5 minute
        run: echo "Hello Github Action Schedule"
