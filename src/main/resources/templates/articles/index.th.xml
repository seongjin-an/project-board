<?xml version="1.0" ?>
<thlogic>
    <attr sel="#header" th:replace="header :: header1"></attr>
    <attr sel="#footer" th:replace="footer :: footer1"></attr>

    <attr sel="#article-table">
        <attr sel="tbody" th:remove="all-but-first"> <!-- th:remove 는 첫번째 요소만 빼고 모두 지우겠다는 의미 -->
            <attr sel="tr" th:each="article : ${articles}">
                <attr sel="td.title/a" th:text="${article.title}" th:href="@{'/articles/' + ${article.id}}"/>
                <attr sel="td.hashtag" th:text="${article.hashtag}"/>
                <attr sel="td.user-id" th:text="${article.nickname}"/>
                <attr sel="td.created-at/time" th:datetime="${article.createdAt}" th:text="${#temporals.format(article.createdAt, 'yyyy-MM-dd')}"/>
            </attr>
        </attr>
    </attr>
</thlogic>